import { View, Text } from 'react-native'
import React from 'react'
import { styles } from '../../styles/reportScreenStyles/reportScreenStyles'

const TakenText = () => {
  return (
    <View>
      <Text style={styles.takenText}>Taken</Text>
    </View>
  )
}

export default TakenText