import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  addTouchButton: {
    bottom: 0,
    right: 4,
    position: 'absolute',
    alignSelf: 'flex-end',
    justifyContent: 'center',
  },
  floatingButton: {
    resizeMode: 'contain',
    width: 80,
    height: 80,
  },
});
